{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.twalk.dev/cloudevents/v1/persona.reply.approved.schema.json",
  "title": "fr.linagora.twalk.persona.reply.approved.v1",
  "description": "A suggested reply was approved by the user and should be sent back to the external network through the bridge that owns the target portal room. Emitted by the Companion Gateway, which serves the approval endpoint an oversight interface (the Companion, Buzz's Dialogue Room) calls: the approval is refused unless the sender's consent is still 'granted' at that moment, and the Gateway is the single writer of consent state, so the check is a read of its own journal rather than a question asked of another service (ADR 0022). 'source' still names the persona whose suggestion was approved, not the publisher. This is the only v1 event that triggers an outbound message.",
  "type": "object",
  "required": [
    "specversion",
    "id",
    "source",
    "type",
    "time",
    "subject",
    "datacontenttype",
    "network",
    "connection",
    "consent",
    "data"
  ],
  "additionalProperties": false,
  "properties": {
    "specversion": {
      "type": "string",
      "const": "1.0",
      "description": "CloudEvents specification version. Fixed to 1.0 for all Twalk v1 events."
    },
    "id": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "Deterministic identifier computed as sha256(suggestion_event_id + ':' + approved_by). A given suggestion is approved at most once by a given user."
    },
    "source": {
      "type": "string",
      "format": "uri-reference",
      "pattern": "^hermes://[^/]+/personas/[a-z0-9_-]+$",
      "description": "URI identifying the persona whose suggestion was approved. Format: hermes://<domain>/personas/<persona_id>.",
      "examples": [
        "hermes://twalk.example.com/personas/assistant"
      ]
    },
    "type": {
      "type": "string",
      "const": "fr.linagora.twalk.persona.reply.approved.v1",
      "description": "CloudEvents type. Reverse-DNS convention with an explicit version suffix. Any breaking change to this schema requires incrementing the suffix (v2, v3, ...) rather than mutating v1."
    },
    "time": {
      "type": "string",
      "format": "date-time",
      "description": "RFC 3339 timestamp of when Hermes produced the event."
    },
    "subject": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "CloudEvents id of the approved suggestion (a persona.suggest.produced event)."
    },
    "datacontenttype": {
      "type": "string",
      "const": "application/json",
      "description": "Fixed to application/json for all Twalk v1 events."
    },
    "dataschema": {
      "type": "string",
      "format": "uri",
      "description": "Optional URI pointing back to this JSON Schema. Recommended for third-party consumers that validate events dynamically.",
      "examples": [
        "https://schemas.twalk.dev/cloudevents/v1/persona.reply.approved.schema.json"
      ]
    },
    "traceparent": {
      "type": "string",
      "pattern": "^[0-9a-f]{2}-[0-9a-f]{32}-[0-9a-f]{16}-[0-9a-f]{2}$",
      "description": "Optional W3C Trace Context traceparent header, normally continued from the trigger event."
    },
    "network": {
      "$ref": "definitions/network.schema.json",
      "description": "Twalk extension. Network the reply will be sent to, copied from the trigger event for server-side filtering on NATS."
    },
    "connection": {
      "$ref": "definitions/connection.schema.json",
      "description": "Twalk extension (ADR 0033). The **connection** this event belongs to — the one configured account the deployment observed it on or acts through — by the id the Companion Gateway's registry gave it. `network` says what kind of thing that is; this says which one. Handed to the producer, never derived by it; see definitions/connection.schema.json."
    },
    "consent": {
      "type": "string",
      "enum": ["granted", "pending", "revoked"],
      "description": "Twalk extension. Consent state of the original sender, copied from the trigger event."
    },
    "data": {
      "type": "object",
      "required": ["persona_id", "suggestion_event_id", "approved_by", "final", "target"],
      "additionalProperties": false,
      "properties": {
        "persona_id": {
          "type": "string",
          "pattern": "^[a-z0-9_-]+$",
          "description": "Identifier of the persona whose suggestion was approved.",
          "examples": ["assistant"]
        },
        "suggestion_event_id": {
          "type": "string",
          "pattern": "^[a-f0-9]{64}$",
          "description": "CloudEvents id of the approved persona.suggest.produced event. Identical to the top-level subject."
        },
        "approved_by": {
          "type": "string",
          "pattern": "^@[a-zA-Z0-9._=/+-]+:[^/]+$",
          "description": "Matrix user ID of the human who approved the reply. The audit trail of who sent what always goes through this field.",
          "examples": ["@michel:example.com"]
        },
        "final": {
          "type": "object",
          "required": ["body", "format"],
          "additionalProperties": false,
          "properties": {
            "body": {
              "type": "string",
              "maxLength": 65536,
              "description": "The exact content to send. May differ from the suggestion when the user edited it before approving."
            },
            "format": {
              "type": "string",
              "enum": ["text/plain", "text/markdown", "text/html"],
              "description": "Content type of the body field. text/plain is the default and safest."
            }
          },
          "description": "The final content to send, as approved."
        },
        "edited": {
          "type": "boolean",
          "description": "True when the final content differs from the approved suggestion. Lets oversight interfaces measure how often personas are corrected."
        },
        "target": {
          "type": "object",
          "oneOf": [
            {
              "required": ["room_id"],
              "additionalProperties": false,
              "properties": {
                "room_id": {
                  "type": "string",
                  "pattern": "^![A-Za-z0-9]+:[^/]+$",
                  "description": "Matrix ID of the portal room the reply must be posted to; the bridge owning the room performs the outbound send, and the Sensor posts it as the owner's device (ADR 0025, ADR 0034)."
                },
                "reply_to_event_id": {
                  "type": "string",
                  "pattern": "^\\$[A-Za-z0-9_-]+$",
                  "description": "Optional Matrix event ID the reply should be threaded under, normally the original inbound message."
                }
              },
              "description": "A reply into a portal room: the Sensor's to send."
            },
            {
              "required": ["connection", "in_reply_to"],
              "additionalProperties": false,
              "properties": {
                "connection": {
                  "$ref": "definitions/connection.schema.json",
                  "description": "The mail connection the reply leaves from (#278): the collector holding it is the one that sends, from the owner's own mailbox, with the grant that already is the owner (ADR 0037). Mirrors the envelope's `connection` extension."
                },
                "in_reply_to": {
                  "type": "string",
                  "maxLength": 998,
                  "pattern": "^<[^<>\\s]+>$",
                  "description": "The Message-ID of the mail being answered — the trigger's `data.message_id` — as the reply's In-Reply-To header will carry it. It is also how the collector finds the mail again to address the reply: to the sender alone, under `Re:` and the original subject, with References continued. Reply-all is a decision the owner did not take in v1."
                }
              },
              "description": "A reply to a mail: the collector's to send."
            }
          ],
          "description": "Where the approved reply must be sent — a portal room for a bridged network, a mail connection for email (ADR 0033: one shape per kind). Whichever component holds the target sends it; the other acknowledges the approval as not its own and leaves it alone, so an approval is never dead-lettered for being the other's."
        }
      }
    }
  }
}
